# my take on ECS in C++
- uses sparse sets
```sh
cmake --preset debug
cmake --build --preset debug && ./build/main
```
```sh
cmake --preset release
cmake --build --preset release && ./build_release/main
```
storing this on git just for me

## AI excuses:
i am really sorry for consulting with ai about src/ecs/registry.cpp but i feel like c++ templates are not made for human mind :-(


the good thing is that i **can** understand what's happening there, so i'll try to investigate the code more.
